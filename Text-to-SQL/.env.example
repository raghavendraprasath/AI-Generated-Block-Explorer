# Copy to .env and fill in your key (never commit .env):
#   cp .env.example .env
#
OPENROUTER_API_KEY=sk-or-v1-your-key-here

# Optional — faster than openrouter/free (still free-tier example):
# OPENROUTER_MODEL=google/gemma-2-9b-it:free

# Optional default database path for web_ui.py / scripts
HW3_DB_PATH=/home/adity/hw3-data/blockchain.db
